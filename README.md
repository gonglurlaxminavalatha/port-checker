# Port Checker using Python

This Python project checks whether a specific port on a given system is open or closed.

Features:
- Takes host name or IP address as input
- Takes port number as input
- Checks connection using socket
- Displays whether port is OPEN or CLOSED

Example:
google.com + port 80 → OPEN
